
//Running with Python version 3.6.5 and dependencies installed locally make sure to have all versions.txt installed before hand

C:\Users\haris\anaconda3\envs\bert\python.exe test.py --config app/config/config.json

//Running with docker image 

//Make sure to send correct path as your machine URL add a -d if you want to run it in the background

//Make sure to replace API url with your ipconfig url localhost

docker pull pakmedic/bert_microservice:latest
docker run -p 5001:5001 -e API_URL=http://192.168.100.14:8000 pakmedic/bert_microservice


//Make changes in code and repush images? 

->Prerequisites
1. Docker installed
2. Docker hub account (pakmedic INC)
3. Download models folder from pakmedic drive BERT


docker build -t pakmedic/bert_microservice .
docker push 